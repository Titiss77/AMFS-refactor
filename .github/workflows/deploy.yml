name: Deploiement vers ByetHost FTP

on:
  push:
    branches:
      - main

jobs:
  web-deploy:
    name: 🎉 Déploiement FTP
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Récupération du code
        uses: actions/checkout@v3

      - name: 📦 Nettoyage du vendor (Production)
        # Remplace 'mathis.likesyou.org/htdocs' par le chemin réel si nécessaire
        working-directory: mathis.likesyou.org/htdocs
        run: composer install --no-dev --optimize-autoloader

      - name: 📂 Synchronisation FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftp
          # Remplace cette ligne par le dossier exact où se trouve ton site chez ByetHost
          # Souvent c'est "htdocs" ou "public_html"
          server-dir: /
          
          # Indique le chemin relatif vers ton fichier de suivi
          state-name: htdocs/.ftp-deploy-sync-state.json
          
          exclude: |
            **/.git*
            **/.git*/**
            **/.github/**
            tests/**
            user_guide_src/**
            phpunit.xml.dist
            .env
            spark
            README.md
            LICENSE
            composer.json
            composer.lock
            vendor/phpunit/**
            vendor/fakerphp/**
            vendor/symfony/**  # Souvent inutile en production
            vendor/mockery/**  # Inutile en production